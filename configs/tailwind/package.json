{
    "name": "@molecula-monorepo/tailwind-config-shared",
    "version": "1.0.0",
    "private": true,
    "description": "Shared tailwind CSS configuration",
    "main": "index.ts",
    "files": [
        "index.ts",
        "theme.css"
    ],
    "scripts": {
        "bump-to": "yarn version --no-commit-hooks --no-git-tag-version --new-version",
        "tsc": "echo workspace does not contain ts code",
        "prettier:check": "prettier --check .",
        "prettier:fix": "prettier --write .",
        "eslint:check": "echo workspace does not contain any code",
        "eslint:fix": "echo workspace does not contain any code"
    },
    "devDependencies": {
        "tailwindcss": "^3.4.12"
    },
    "volta": {
        "extends": "../../package.json"
    }
}
